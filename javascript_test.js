/* 
Javascript is dynamically typed
You don't have to declare the variable type
console.log("Hello" + ' ' + 'world') prints to the terminal window

Writing a function looks like:
function join(a, b) {
return a + ' ' + b;
}

*/